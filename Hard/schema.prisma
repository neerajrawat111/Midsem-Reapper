generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// Create you model here
model User {
  id          Int    @id @default(autoincrement())
  name        String
  email       String @unique
  connections Json
}
